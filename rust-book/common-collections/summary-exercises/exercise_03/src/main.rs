use exercise_03::interface::Interface;

fn main() {
    println!(
        "\n- The conclusion of 'Exercise 03' -\n"
    );

    let mut interface = Interface::new();

    interface.show_the_interface();
}