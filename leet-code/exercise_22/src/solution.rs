pub struct Solution {}

impl Solution {
    pub fn generate_parenthesis(
        n: i32
    ) -> Vec<String> {
        let result: Vec<String> = Vec::new();

        if n <= 1 && n >= 8 {
            
        }

        result 
    }
}